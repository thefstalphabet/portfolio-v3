import styled from 'styled-components'

export const Container = styled.div`
    height: auto;
    padding: 10px 1rem;
    background-color: var(--sec-color);
    text-align: center;
    h2, p {
        color: var(--white);
    }
`;